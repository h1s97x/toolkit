/**
 * @h1s97x/api-client
 *
 * Type-safe API utilities for both server and client.
 *
 * - `response`: Unified response format builder (Next.js, server-side)
 * - `client`: HTTP client with auth token, timeout, and dual-format parsing (browser, client-side)
 */

export * from './response';
export * from './client';
